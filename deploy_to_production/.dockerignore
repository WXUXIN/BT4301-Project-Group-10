__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.ipynb_checkpoints/
.git/
.gitignore

# Artifacts are mounted at runtime — never baked into the image
artifacts/

# Sync script runs on the host, not inside the container
sync_champion_from_mlflow.py

# Docs
README.md
